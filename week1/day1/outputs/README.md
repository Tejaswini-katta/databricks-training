# SQL Practice - Day 1 Data Log
This folder contains the result sets obtained from running the queries defined in `queries.sql` on the provided database schema. 

The results have been verified against the expected outcomes for the following topics:
- **Basic Queries:** All CRUD-like select operations completed and verified.
- **String Matching:** Pattern matching (`LIKE`, `CHAR_LENGTH`, `SUBSTR`) successfully validated.
- **Date Queries:** Temporal filtering (`YEAR`, `MONTH`, `DATE_ADD`) confirmed to match dataset parameters.
- **Aggregate & Grouping:** Statistical summaries (`SUM`, `AVG`, `COUNT`, `GROUP BY`, `HAVING`) confirmed accurate.

*Note: Results were verified using DB Fiddle (MySQL 5.7).*
